package com.google.gwt.user.client.rpc;

/**
 * This is a huge hack for testing.  There isn't a set of GWT libraries in a Maven Repo yet, so
 * we're putting in this interface to make it easier for our unit tests to build their generated files.
 */
public interface IsSerializable {
}
